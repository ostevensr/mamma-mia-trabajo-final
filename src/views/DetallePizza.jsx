
export default function DetallePizza() {

    const context = useContext(AppContext);

    const { nombre } = useParams();


    return (
        <div>

            <h1>Test</h1>
            
        </div>

    );
}